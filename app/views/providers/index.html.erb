<%= render 'form' %>

<section class="content invoice">
  <h1>Proveedores</h1>

  <table id="cities" class="display">
    <thead>
    <tr>
      <th>Nombre</th>
      <th>Rut</th>
      <th>Teléfono</th>
      <th>Email</th>
      <th>Ciudad</th>
      <th>Region</th>
      <th>Editar</th>
    </tr>
    </thead>
    <tbody>
    <% @provider.each do |t| %>
        <tr>
          <td><%= t.provider_name %></td>
          <td><%= t.provider_rut %></td>
          <td><%= t.provider_tel%></td>
          <td><%= t.provider_email %></td>
          <td><%= t.city.city_name %></td>
          <td><%= t.region.region_name %></td>
          <td><%= link_to 'Editar', edit_provider_path(t) %></td>
        </tr>
    <%end%>
    </tbody>
  </table>
</section>