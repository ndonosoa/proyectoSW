<% content_for :content_header  do %>
  <section class="content-header">
    <h1>Regions <small>#index</small></h1>
  </section>
<% end %>

<section class="content invoice">
  		<% @region.each do |t| %>
  		 <div>
 			<%= t.region_name%>
 			<%= t.id %>
 		 </div>
 		<%end%>
</section>